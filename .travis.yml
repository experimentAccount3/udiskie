language: python
python:
  - "2.7"

before_install:
     # add for profile
    - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
    - sudo apt-get update -qq


install:
  # add for profile
    - sudo apt-get install -qq g++-4.8
    - export CXX="g++-4.8"
    - pip install nose-cprof
    - pip install properform

    - pip install . --use-mirrors
script:
  - nosetests --with-cprofile --cprofile-stats-erase --cprofile-stats-file=profile_udiskie_d9b04fca8a_0
  - python -m properform Push 47.88.50.110:15554 asdf_udiskie_d9b04fca8a_0 ./profile_udiskie_d9b04fca8a_0
 #2017-09-18 14:18:50 modified at 0